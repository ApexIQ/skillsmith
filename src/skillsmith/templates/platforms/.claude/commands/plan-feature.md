Create a feature plan for the current task.

Steps:
1. Read `CLAUDE.md`, `.agent/STATE.md`, and `.agent/project_profile.yaml`.
2. Ask the `researcher` subagent to inspect relevant files and skills.
3. Produce a short implementation plan with verification steps.
