name: test-union-body
version: 1.0.0
description: Autonomously discovered prototype from test_union_body.py
logic:
  discovered_patterns:
  - Analyzed .py file with 134 lines.
  - Detected high-density logical structures (classes/interfaces).
  - Self-evolving agents should prioritize this pattern for similar system designs.
  core_implementation_rules:
  - Match the structural integrity of test_union_body.py.
  - Ensure separation of concerns as seen in the original source.
embodiments:
  claude:
  - Implement with strict type safety as observed in test_union_body.py.
  gemini:
  - Use descriptive documentation matching the patterns found in test_union_body.py.
